# Origin of the CORS proxy built from ../gateway. Required for production
# builds (`npm run build`); dev mode ignores this and proxies through Vite.
VITE_NOTE_PROXY_BASE=https://note-proxy.example.com/api
